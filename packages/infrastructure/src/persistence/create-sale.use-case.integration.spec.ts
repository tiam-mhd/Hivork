import { describe, it } from 'vitest';

/**
 * Full PG integration coverage:
 * `packages/application/src/installments/sales/create-sale.integration.spec.ts`
 */
describe.skip('CreateSaleUseCase integration (see @hivork/application)', () => {
  it('runs against PostgreSQL in the application package', () => {});
});
